// Replaced by AmbientPool — keeping this as a no-op to avoid import errors
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function SectionGlow(_props: { intensity?: string; position?: string }) {
  return null;
}
